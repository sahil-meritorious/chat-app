# Leave empty to route through the Vite dev proxy (see vite.config.js) and
# avoid CORS/preflight issues entirely during development.
# Set to a full URL (e.g. https://rag-application-q614.onrender.com) only if you are NOT proxying
# and your backend has CORS middleware configured to allow this origin.
VITE_API_BASE_URL=
